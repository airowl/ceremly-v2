# Migration Gates Ledger

| Gate | Name | Status | Command | Evidence | Approved by | Approved at |
|------|------|--------|---------|----------|-------------|-------------|
| G01 | Cloudflare/Nuxt | PASS | `pnpm build:cloudflare && pnpm preview:cloudflare` | docs/migration/evidence/G01-cloudflare.md | — | — |
| G02 | Vue binding | PASS | `pnpm test:gate:g02` | docs/migration/evidence/G02-convex-vue.md | — | — |
| G03 | Password | PASS | `pnpm test:gate:g03-g05` | docs/migration/evidence/G03-G05-auth.md | — | — |
| G04 | Google | NOT_RUN | `pnpm test:gate:g03-g05` | docs/migration/evidence/G03-G05-auth.md | — | — |
| G05 | 2FA | PASS | `pnpm test:gate:g03-g05` | docs/migration/evidence/G03-G05-auth.md | — | — |
| G06 | org/RBAC | PASS | `pnpm test:gate:g06` | docs/migration/evidence/G06-org-rbac.md | — | — |
| G07 | Creem | PASS | `pnpm test:gate:g07` | docs/migration/evidence/G07-creem.md | — | — |
| G08 | Media | PASS | `pnpm test:gate:g08` | docs/migration/evidence/G08-media.md | — | — |
| G09 | Protezioni | PASS | `pnpm test:gate:g09` | docs/migration/evidence/G09-protections.md | — | — |
| G10 | Costi | NOT_RUN | `pnpm test:migration -- load-model && pnpm tsx scripts/migration/load-runner.ts` | docs/migration/cost-model.md | — | — |

**Gate order:** G01 → G02 → G03 → G04 → G05 → G06 → G07 → G08 → G09 → G10
**Gate rule:** The next phase does not start until the previous gate is `PASS`. A failed gate remains `FAIL` with evidence and blocks migration; the requirement is not changed to make it pass.

**Notes:**
- G03: PASS (2026-09-18, staging `airowl/ceremly-staging` → `wary-spaniel-466`) — the production chain was exercised end-to-end on synthetic-but-faithful accounts: legacy Neon rows → AES-256-GCM encrypted `MigrationBatch` → `internal.migrations.authImport.importBatch` → sign-in on Convex Better Auth. Cases: password sign-in with the imported hash (plus a wrong-password negative control), logout invalidating the session and a second login, replay of the same batch writing nothing (idempotency), and the Better Auth JWT accepted as a Convex identity. `Approved by` stays `—`: this is mechanism evidence on staging, not the human sign-off (GO/NO-GO, Tasks 17–18).
- G04: NOT_RUN — the imported `google` account row is preserved and linked to the same user (verified live after a password sign-in via `/api/auth/list-accounts`), but the OAuth round trip itself needs a real Google consent screen and a staging redirect URI, so the requirement is unexecuted, not failed. Blocked on the minimized production copy + staging Google client.
- G05: PASS (2026-09-18, same deployment) — the imported (`symmetricEncrypt`-ed) TOTP secret validates a code computed by an independent RFC 6238 implementation outside the auth library, and the imported (`"encrypted"` by default) backup codes are consumed exactly once: first use succeeds, replay is rejected, and an untouched code still recovers. Handoff to Task 5+: `BETTER_AUTH_SECRET` must not rotate at cutover (2FA payloads are encrypted with it) and the Better Auth component schema is fixed, so `globalRole`/profile fields belong to `appUsers`/domain tables.
- G06: PASS (2026-09-21, same deployment) — organizations, memberships and invitations are application-domain Convex tables with server-side RBAC: 36 hermetic `convex-test` cases (cross-tenant reads, escalation attempts, invitation theft/expiry/replay, concurrent accepts converging on one membership, audit on every write) plus a live sign-up on staging that materialised `appUsers` + personal organization + owner membership through the Better Auth `user.create` trigger. Capabilities mirror the Better Auth organization plugin defaults while keeping the legacy rule that every member writes domain resources. Handoffs: `ensureProvisioned` is the self-heal path for users that predate the trigger (observed live), `inviteMember` returns the plaintext token for Task 13 to deliver, and the `expired` invitation status is reserved for that task's cron sweep. `Approved by` stays `—`: evidenced, not human-signed.
- G06 handoff, **not** closed by Task 13 (2026-09-22) — correction of the note above. Task 13 delivered the delivery machinery (`JOB_TYPES.sendInviteEmail = "send-invite-email"`, 5 attempts, `convex/emailTemplates/OrgInviteEmail.ts` moved and rendered through the Resend action) but that job type is the **guest** invitation; no cron sweeps `expired` invitations and nothing enqueues an organization-invitation email. `organizations.inviteMember` still returns a plaintext token and stops there, because the body must link to `/invite/*` and that route is Task 14's frontend contract; `convex/lib/jobQueue.ts` records the deferral explicitly (org invite is not a registered job type, so `enqueueJob` would refuse it rather than queue a job nobody runs). Consequence, stated rather than hidden: **an organization invitation created through the Convex backend today is silent** — it works only if the token is transported by hand. The `expired` sweep stays hygiene rather than correctness, since expiry is already evaluated on read.
- G06 handoff, delivery half **closed by Task 14 part b (2026-09-24)**: `organizations.inviteMember` now queues a durable `send-org-invite-email` job (payload `{ invitationId }` only, dedupe per invitation, Resend idempotency key) whose email links `{SITE_URL}/invite/{token}`; the token is derived as HMAC(`BETTER_AUTH_SECRET`, `org-invite:{id}`) so no plaintext is stored anywhere, and a hash mismatch is a terminal skip. Evidence: `convex/orgInvites.test.ts` (11 cases, hermetic). The `expired` sweep remains open (hygiene). Details: `docs/migration/frontend-convex.md` §4ter.
- G07: PASS (2026-09-21, same deployment, Creem **test mode**) — the billing entity is always the caller's active organization, resolved server-side: 22 hermetic `convex-test` cases (RBAC refusals before the provider is read, no client-supplied `entityId`, unforgeable reserved metadata keys, one ledger row per provider event, refund/late-completion ordering) plus 7 live cases on staging (a real test-mode checkout, a signature-verified completion that unlocks exactly once, a redelivery that writes nothing, a refund that re-locks and keeps the order id, an invalid signature rejected with `403`, portal resolution). Measured: `checkouts.create` returns no `customer` (Creem creates it at payment; the completion webhook mirrors it), the SDK silently defaults to the **production** API unless `CREEM_SERVER` is derived from the key prefix, and `registerRoutes` hands the handler an *already parsed* event — re-parsing it made every real checkout `ignored`, now pinned by a test through the real HTTP route. `Approved by` stays `—`: evidenced, not human-signed.
- G09: PASS (2026-09-21, Worker locale dal bundle Cloudflare + staging `airowl/ceremly-staging`) — matrice completa in `docs/migration/protection-matrix.md`, 36 casi di gate (`security-headers` 15, `convex/lib/rateLimit` 14, `convex/auth` 3) più le prove live. Il limiter Convex è atomico (check e incremento in una sola mutation; 12 chiamanti concorrenti con budget 4 ne ammette esattamente 4), memorizza solo `sha256(bucket\0key)` ed è a finestra fissa allineata all'epoca. Misurato: (a) le route servite dalla **Assets layer** di Workers non ricevevano alcun header di sicurezza — `GET /` senza HSTS e `/_nuxt/**` senza `immutable`, una regressione che Vercel non aveva — risolto con `public/_headers`; (b) `/api/auth/*` è un proxy trasparente e non porta header applicativi, quindi la riga auth appartiene all'edge; (c) la config Convex di Better Auth aveva **perso** le regole brute-force legacy (default `memory`, per-isolata su serverless): riportate verbatim con `storage: "database"`, verificato live (10 tentativi ammessi, 11° → `429` con `retry-after: 60`, recupero dopo la finestra, contatore nella tabella `rateLimit` del componente); (d) nessuna regola WAF/Cloudflare Rate Limiting è dichiarata nel repo — le celle "edge" della matrice sono obblighi di deploy, e finché non esistono l'enforcement sta in Convex. `Approved by` resta `—`: evidenza, non firma.
- G08: PASS (2026-09-21) — files and image variants are Convex domain behind an HMAC-signed bridge: 32 hermetic cases (authorization ordering, magic bytes before `ready`, tenant-scoped dedup, two-variants-max state machine with five attempts and a terminal `failed`, cross-runtime signing contract) plus a live run of the built Worker on `wrangler dev`, where a correctly signed presign returns a real R2 URL while unsigned/stale/tampered/replayed requests are all refused, and a seeded 51 kB PNG produced `thumb.webp` (4.7 kB) and `web.webp` (19.2 kB) in the legacy key layout through the Images binding. Measured: the `runtimeConfig` singleton is empty on a Worker (its `process.env` fallback) so the bridge now uses `useRuntimeConfig()` — a Task 19 blocker for the rest of the legacy server code; `@cloudflare/workers-types` was not installed, so the existing triple-slash reference had been resolving to nothing. Not executed: the Convex → Worker leg and the full round trip, which need a deployed Worker (Task 16). `Approved by` stays `—`: evidenced, not human-signed.
- G01: PASS — Config test, client+server build, 79 prerendered routes, Worker entry, dry-run bindings, live SSR/headers/bot-trap verified on `wrangler dev`. sharp aliased to stub for cloudflare preset only (server/utils/sharp-stub.ts); real Images pipeline in Task 7.
- G02: PASS (2026-09-18) — verified against the staging dev deployment `airowl/ceremly-staging` → `wary-spaniel-466` (eu-west-1). All Task 3 Step 4 cases pass: public query with `suspense()` on SSR, CSR `server: false` opt-out, typed mutation, realtime re-execution after a write, authenticated query identity (gate-only RS256 `customJwt` provider), anonymous fallback, exactly one forced token refresh, and survival of a transient session failure. Two constraints handed to Task 4: `installConvex` must call `setAuth` itself (convex-vue ignores its `auth` option) and `fetchToken` must never reject (unhandled rejection, client never connects). `Approved by` stays `—`: the gate is evidenced, not human-signed; sign-off belongs to GO/NO-GO (Task 17–18).
- G10: NOT_RUN (2026-09-21, modello su volumi misurati live su `wary-spaniel-466`) — il gate è una congiunzione: *fascia scelta esplicitamente* **e** *budget/usage alert configurati*. Solo il primo termine è soddisfatto. La fascia è scelta e documentata in `docs/migration/cost-model.md` (Convex Free/Starter fino a ~12,5M call/mese, Workers Paid da 1.000 planner, Resend Pro da 20 planner, Cloudflare Images Free fino a 5.000 trasformazioni); gli alert **non sono configurabili da questo repository**: i limiti di spesa Convex esistono solo nella dashboard (`convex --help` non ha alcun comando di budget, verificato) e l'alerting Cloudflare richiede un API token, mentre in `.env` ci sono solo le chiavi S3 di R2, che non autenticano la REST API. Registrato `NOT_RUN` per la stessa ragione di G04: non eseguito, non fallito — nessuna evidenza lo contraddice e nessun requisito è stato ammorbidito per farlo passare. Misurato (non dedotto): il fan-out reattivo è **esattamente una ri-esecuzione per subscriber** a 1/5/20 client connessi e **zero** per una scrittura che non cambia il risultato; a 1.000 planner il modello dà **278.050 call** (41.550 = 15% reattive), di cui la costante del piano (1.000 call/planner → 1.000.000 call) avrebbe sbagliato di 3,6× e avrebbe argomentato il tier Professional su un numero mai misurato; **l'email è il 94% del conto** (Resend $245 su $260) mentre il compute Convex sono tre centesimi; e i due driver sono distinti — guest/reminder muovono il conto (+52%) senza toccare il contatore delle call, 20 subscriber concorrenti moltiplicano le call (+284%, oltre il milione incluso) per 58 centesimi. Bloccato su: accesso alla dashboard (le quattro soglie sono tabulate nel documento).